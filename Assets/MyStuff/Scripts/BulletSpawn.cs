using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletSpawn : MonoBehaviour
{
    /*public GameObject boomEffect;
    public GameObject lostBulletEffect;*/

    void Awake()
    {
        /*var theBoom = Resources.Load("MuzzleFlashEffect");
        boomEffect = theBoom as GameObject;

        var emptyShell = Resources.Load("CartridgeEjectEffect");
        lostBulletEffect = emptyShell as GameObject;*/
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
